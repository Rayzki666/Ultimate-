{
  "name": "frame-ai-camera",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Frame AI camera web app and Capacitor iOS shell",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "ios:generate": "npm run build:web && cap add ios --packagemanager SPM && node scripts/configure-ios.mjs",
    "ios:sync": "npm run build:web && cap sync ios && node scripts/configure-ios.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@capacitor/core": "8.5.2",
    "@capacitor/ios": "8.5.2",
    "@capacitor/haptics": "8.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2",
    "esbuild": "0.28.2"
  },
  "engines": {
    "node": ">=22"
  }
}
